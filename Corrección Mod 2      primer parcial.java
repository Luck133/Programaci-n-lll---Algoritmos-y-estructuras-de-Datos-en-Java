private boolean seguidores (vertice<String> V){

int contador=0;
L=this.listaDeVertices();
for (int i=0;i<=L.tamanio){
              if (esAdyacente(L.elemento(i)), v) //ACA LUCIO DIJO QUE "los seguidores son los vértices que tienen un arista de salida hacia nosotros, es decir, deberías hacer "Si el elemento actual apunta a V, sumo un seguidor"//
                         contador++;   // En otras palabras, si desde el 1er vértice "L", o el que venga de la lista que tengo, apunta a mi vertice V actual
						               //  ( V como referencia que paso), entonces "L" es "Mi Seguidor" 
                          }
						  
						  
if contador (contador>100) && (contador <200)
                 return true;
else
                 return false;
}